import React from "react";
import '../App.css';


const BlueButton = ({btnText}) => {
  return (
    <button
      className={``} onClick={``}
    >
      {btnText}
    </button>
  );
}

export default BlueButton